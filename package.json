{
  "name": "xarop.com",
  "version": "2026.0.0",
  "description": "xarop.com — static site, no database, Markdown-based. Six flavors.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx --yes http-server dist -p 4000 -c-1 -o",
    "dev": "npm run build && npm run serve",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.7.1",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "turndown": "^7.2.4"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "xarop <ajl@xarop.com>",
  "homepage": "https://xarop.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/xarop/web.git"
  }
}
